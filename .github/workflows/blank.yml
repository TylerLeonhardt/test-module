name: CI

on: [push]

jobs:
  build:

    runs-on:
    - ubuntu-latest
    - windows-latest
    - macOS-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: pwsh -c '$PSVersionTable'
